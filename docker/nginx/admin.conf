# 管理端 admin.ljl0628.cn：整站转给 admin 容器（容器自带 nginx 托管静态文件并反代 /api）。

# ── HTTP（HTTPS 上线后替换为 return 301，同 blog.conf 的做法）──
server {
    listen 80;
    server_name admin.ljl0628.cn;

    location / {
        proxy_pass http://admin:80;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ── HTTPS（阶段 4 放开；证书与主域一起签发或用通配符证书）──
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name admin.ljl0628.cn;
#     ssl_certificate     /etc/nginx/certs/admin.ljl0628.cn.pem;
#     ssl_certificate_key /etc/nginx/certs/admin.ljl0628.cn.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     location / {
#         proxy_pass http://admin:80;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
